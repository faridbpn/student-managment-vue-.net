version: '3.8'
services:
  backend:
    build: ./SimpleApi
    ports:
      - "5000:80"
    environment:
      - ASPNETCORE_URLS=http://+:80
  frontend:
    build: ./frontend
    ports:
      - "8080:80"